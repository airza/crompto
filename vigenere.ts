//split code word

//for first letter,
    //get every xth letter from text
    //caesar rotate
//(more letters)

//recombine

//


/*
NOP
SPHINX THING IDK
012345 67890 123

N
S, I, T, N, D
{0, 3, 6, 9, 12}

O
array all the bois +1


 */

import {Frequencies} from "./caesar";

let codeword:string = "NOP"

export function indexCodeword(textPosition:number, codeword:string): number {
    let codewordLength:number = codeword.length
}